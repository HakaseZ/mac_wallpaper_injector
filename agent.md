# Agent 规范 (agent.md)

## 系统操作确认规范

1. **对用户系统状态有影响的操作,必须先获得用户明确确认,不得自行执行。**
   包括但不限于:修改系统文件(如 `~/Library/Application Support/com.apple.wallpaper/`)、更改壁纸/Index.plist、重启系统进程(WallpaperAgent 等)、删除用户数据、安装/卸载软件。
2. 用户说"在我确认前不要执行"时,立即停止执行,等待用户确认后再继续。
3. 只读侦察(查看文件、读日志、ffprobe 探测等)不需要确认。
4. 涉及系统状态变更前,先备份并说明可逆性,再请求确认。
5. 执行用户已明确确认的操作时,按确认的范围执行,不擅自扩大。

## 测试文件状态记录规范

6. `test_videos/` 下的测试文件若有任何改动(新增、删除、替换、重新转码等),**必须同步更新 `test_videos/TEST_FILES.md`**,按注入需求规格(见 `INJECTION_REQUIREMENTS.md`)重新核对并更新对应文件的规格状态。
7. 每次核对应以实际探测结果为准(ffprobe / NAL 分析),不得凭记忆或推断填写状态。

## 实验原则

8. **每次实验前先明确实验基线,实验完成后必须恢复至基线。**
9. 实验文档(`EXPERIMENTS.md`)必须记录每次实验的基线,以及实验开始前的基线(即上一次实验恢复后的状态)。
10. 实验文档必须在每次实验结束后 commit 到 git。
11. 每次实验后不保留上次的实验记录,追踪交给 git commit 历史完成(文档只保留当前实验内容)。
12. 每次实验后,将结论(日期/结论/根因/证据/影响/恢复情况/下一步)追加到 `EXPERIMENT_CONCLUSIONS.md`;**该文档全量累积、永久保留,不得删除或覆盖**,由 git 全量追踪。

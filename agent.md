# Agent 规范 (agent.md)

## 系统操作确认规范

1. **对用户系统状态有影响的操作,必须先获得用户明确确认,不得自行执行。**
   包括但不限于:修改系统文件(如 `~/Library/Application Support/com.apple.wallpaper/`)、更改壁纸/Index.plist、重启系统进程(WallpaperAgent 等)、删除用户数据、安装/卸载软件。
2. 用户说"在我确认前不要执行"时,立即停止执行,等待用户确认后再继续。
3. 只读侦察(查看文件、读日志、ffprobe 探测等)不需要确认。
4. 涉及系统状态变更前,先备份并说明可逆性,再请求确认。
5. 执行用户已明确确认的操作时,按确认的范围执行,不擅自扩大。

## 测试分层

6. **沙箱测试**(`panel/tests.sh`,默认):单元 + 集成,路径经 `Paths.current` 注入临时目录,不触碰真实系统壁纸状态,可随时运行。
7. **系统级 e2e**(`panel/tests.sh e2e`):注入/选择/播放/删除/恢复真实系统壁纸,结束后自动回基线;需辅助功能权限,运行前须获得用户确认。
8. `test_videos/` 测试文件若有改动(新增/删除/替换/重新转码),须同步更新 `test_videos/TEST_FILES.md`;以实际探测结果为准(ffprobe / NAL 分析),不得凭记忆或推断填写。

## 维护阶段约定

9. 实验阶段已结束;研究记录归档于 `research/`(实验结论、实现研究、注入规格、Python 原型),`EXPERIMENTS.md` 不再入库(gitignore)。
10. 改动涉及注入规格时,同步更新 `research/INJECTION_REQUIREMENTS.md` 对应条目。

## 决策打断规范

11. 遇到需要用户决策的分叉点(方案选择、范围扩展、风险取舍、超出已确认范围的操作),**主动中断当前工作并询问用户**,不要自行选择继续;用户给出决定后再执行。
